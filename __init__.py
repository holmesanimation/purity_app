# ConsoleCapture.start() is called at the top of app.py (before other imports)
# so that all output is captured when launched via `python app.py`.
# This __init__.py is only executed when purity_app is imported as a package.
